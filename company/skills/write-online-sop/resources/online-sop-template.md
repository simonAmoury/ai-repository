# <迭代/功能名> 上线 SOP

> 一句话说明本次迭代做了什么、在什么基础上迭代。
> **DDL 变更：<有 / 无>**（有则简述）。部署对象：`<服务A>`、`<服务B>`。

## 一、上线前需处理项

### 1. Apollo 配置

| 服务 | appid | env | cluster | key | value |
|------|-------|-----|---------|-----|-------|
| pac-foreign | pac-platform | PRO-HWGZ | pac-platform-cluster | `<config.key>` | `<value>` |
| pac-bop-admin | pac-bop | PRO-HWGZ | default | `<config.key>` | `<value>` |

> 配置改完记得在 Apollo **点发布**。

### 2. 外部平台 / 权限

- <平台>需具备<权限>，否则<报错表现>。
- 此为<运营/运维>侧开通项，上线前确认。

### 3. 数据处理（如有）

```sql
-- 仅测试库示例；⚠️ 线上库不执行
```

### 4. 依赖升级回归（如有）

<SDK / 组件 升级说明及回归范围>

---

## 二、上线顺序

1. Apollo 配置改好并发布（第一节）
2. <外部权限确认到位>
3. 部署 `<被依赖的服务>`
4. 部署 `<依赖方服务>`

> <说明部署先后的依赖原因>

---

## 三、上线后自检

- [ ] <可观测结果 1>
- [ ] <可观测结果 2>
- [ ] <可观测结果 3>

---

## 四、回滚

| 项 | 回滚动作 |
|----|----------|
| 应用 | 回滚 `<服务>` 到上一稳定镜像 |
| Apollo | <各新增 key 删除 / 保留> |
| 数据库 | <无 DDL 变更，无需回滚 / 具体回滚 SQL> |
| 其他 | <SDK / 三方约定 随镜像回滚> |

> <回滚性质说明：是否纯应用回滚、有无破坏性数据操作、可否部分回滚>

